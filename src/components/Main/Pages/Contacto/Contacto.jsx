import { Layout } from "../Items/Layout"
import { Form } from "./Form"


export const Contacto = () => {
    return (
        <Layout titulo='Contactanos'>
            <Form />
        </Layout>
    )
}